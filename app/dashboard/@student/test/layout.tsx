export default function StudentSlotLayout({
  children,
}: //   params,
{
  children: React.ReactNode;
  //   params: Record<string, string>; // More generic type to handle any parameter
}) {
  //   console.log(params);
  return <>{children}</>;
}
